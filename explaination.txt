#include <function> 
	function<void(tileArray&, const int, const int)>&& func
#include <vector>
-game.cpp:
	vector<tile> tileRow;
	vector<vector<int>> neighbours;
	vector<int> neighbour = { emptyTileRow + deltaRow, emptyTileCol + deltaCol };
	typedef vector<vector<tile>> tileArray;
	vector<tile> tileRow;
-menu.cpp
	vector<Button> buttons;
#include <cstring>
	void logSDLError(ostream& os, const string& msg, bool fatal = false);
ostream: output stream
msg: message
Mix_OpenAudio(44100, MIX_DEFAULT_FORMAT, 2, 2048
	44100: frequency for 44.1KHz, which is CD audio rate.
	2: number of sound channels in output, set to 2 for stereo
	2048: size of each mixed sample
ifndef define endif: include guard to avoid the problem of double inclusion
SDL_RenderFillRect: đổ màu vào phần còn lại của hcn
?time...
?char numberStr[(((sizeof number) * CHAR_BIT // số bít trong char) + 2) / 3 + 2];
?FPS
?miliSecPerFrame
?function<void(tileArray&, const int, const int)>&& func
?pixelPerSecond
?miliSecPerPixel
?itter_swap
?doneMoving
